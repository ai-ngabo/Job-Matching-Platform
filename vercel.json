{
  "buildCommand": "cd frontend-system && npm ci && npm run build",
  "outputDirectory": "frontend-system/dist",
  "framework": "vite",
  "env": {
    "CI": "true"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
